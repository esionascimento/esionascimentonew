import styled from 'styled-components';
import { Row } from 'antd';

export const RowBody = styled(Row)`
`;

export const Theme = styled.div`
    /* background: ${(props) => (props.theme === 'dark' ? '#292929' : '#F0F2F5')}; */
`;
